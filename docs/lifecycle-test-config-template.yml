---
# generated by terraform
auth_url_v3: <replace-me>
domain: <replace-me>
floating_ip: <replace-me>
default_key_name: <replace-me>
manual_ip: <replace-me> # An ip from net_id network
net_id: <replace-me> # Network UUID
net_id_no_dhcp_1: <replace-me> # Network UUID
net_id_no_dhcp_2: <replace-me> # Network UUID
no_dhcp_manual_ip_1: <replace-me> # An ip from net_id_no_dhcp_1 network
no_dhcp_manual_ip_2: <replace-me> # An ip from net_id_no_dhcp_2 network
project: <replace-me>

# manual envs
username_v3: <replace-me>
api_key_v3: <replace-me>
stemcell_path: <replace-me>
flavor_with_no_root_disk: <replace-me>

# Keystone v2 test options
#auth_url_v2: <replace-me>
#username_v2: <replace-me> # Only used with `auth_url_v2`, falls back to `username_v3`
#api_key_v2: <replace-me> # The v2 password, only used with `auth_url_v2`, falls back to `api_key_v3`
#tenant: <replace-me> # Only used with `auth_url_v2`, falls back to `project`

# Optional configuration:
#region: <replace-me>
#insecure: true
#ca_cert: <replace-me>
#disable_snapshots: true
#config_drive: <replace-me>
#ignore_server_az: 'true'
#instance_type: <replace-me>
#connect_timeout: <replace-me>
#read_timeout: <replace-me>
#write_timeout: <replace-me>
#volume_type: <replace-me>
